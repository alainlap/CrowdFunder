<div class="large-2 columns">
	<%= image_tag(project.img, class: "th") %>
</div>

<div class="large-4 columns">
	<h4><%= link_to project.name, project_path(project) %></h4>
	<p>You contributed <strong>$<%= transaction.dollar_amount %>.00</strong> on <strong> <%= transaction.created_at.strftime("%d-%m-%Y") %></strong></p>
</div>