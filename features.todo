project#new and project#edit should be able to dynamically add and remove tiers, right now it is defaulting to ten tiers per project

each user should only be able to have one transaction per project (if they want to contribute more, they can edit the transaction) ☐ 